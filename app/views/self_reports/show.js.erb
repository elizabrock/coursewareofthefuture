$("#selfreport<%= self_report.date.yday %>").foundation('reveal', 'close');
$('td[data-date="<%= self_report.date.to_date %>"] div.self-report').
  replaceWith("<%= escape_javascript(render self_report) %>");
