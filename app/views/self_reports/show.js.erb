$('td[data-date="<%= self_report.date.to_date %>"]')
  .bind("ajax:success", function() {
    $('td[data-date="<%= self_report.date.to_date %>"] form')
    .replaceWith("<%= escape_javascript(render self_report) %>");
})
