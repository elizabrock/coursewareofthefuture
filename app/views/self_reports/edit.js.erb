$('td[data-date="<%= self_report.date.to_date %>"]').bind("ajax:success", function() {
  $('td[data-date="<%= self_report.date.to_date %>"] div').
    replaceWith("<%= escape_javascript(render(partial: "form")) %>");
  })
