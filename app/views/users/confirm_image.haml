%h2 Avatar
- if current_user.avatar_url
  %img{src: current_user.avatar_url}

  = simple_form_for current_user, url: "/users/#{current_user.id}" do |f|
    = f.input :avatar_confirmed, as: :boolean, label: "This is a picture of me."
    = f.button :submit, "Submit"
